Here is the live website: https://winning-trade.vercel.app/

The idea is to build an app that tracks for real time trade signals based on BoS, CHoCH, Fair value gap, and crypto news. Working for both crypto, forex and memes.... all details are in uploaded folders.

Also, app is meant to send signals to telegram channel.

Issues now is that we can't get the live charts to be working fine. And I am thinking we should use polygon api provider for that since binance is not responding. Considering rate limit, we will be using 4 different keys so if one fails, it falls back on the other.

Also, we need to ensure that live charts for both crypto and forex, etc are working fine. The accuracy of signal calls depends highly on this.

Also to ensure clarity, signals are meant to be strict and guarantee 80% accuracy.  That means that for every signal, there should be confirmation before notification

Check notification sound to be working perfectly.

Check the UI to be more appealing and user friendly.  Make it more beautiful and modern.

Make sure the app is responsive on all devices.

Make sure the app is fast and responsive.

Make sure the app is secure.

Make sure the app is scalable.

Make sure the app is maintainable.

Make sure the app is testable.

Make sure the app is accessible.

Make sure the app is reliable.

Make sure the app is performant.

Make sure the app is secure.

Make sure the app is scalable.

Make sure the app is maintainable.

Make sure the app is testable.

Make sure the app is accessible.

Make sure the app is reliable.

Make sure the app is performant.

Make sure prices are accurate with live prices very important again.